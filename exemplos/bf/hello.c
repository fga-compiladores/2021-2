
#include<stdio.h>

void main() {
    char array[30000] = {0};
    int i = 0;
    while (array[i]) {
    array[i] = getchar();
    putchar(array[i]);
    while (array[i]) {
    putchar(array[i]);
    }
    array[i] = getchar();
    putchar(array[i]);
    putchar(array[i]);
    array[i] = getchar();
    array[i] = getchar();
    array[i] = getchar();
    ++array[i];
    array[i] = getchar();
    --array[i];
    array[i] = getchar();
    --i;
    ++i;
    array[i] = getchar();
    while (array[i]) {
    }
    putchar(array[i]);
    putchar(array[i]);
    }
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    while (array[i]) {
    ++i;
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    while (array[i]) {
    ++i;
    ++array[i];
    ++array[i];
    ++i;
    ++array[i];
    ++array[i];
    ++array[i];
    ++i;
    ++array[i];
    ++array[i];
    ++array[i];
    ++i;
    ++array[i];
    --i;
    --i;
    --i;
    --i;
    --array[i];
    }
    ++i;
    ++array[i];
    ++i;
    ++array[i];
    ++i;
    --array[i];
    ++i;
    ++i;
    ++array[i];
    while (array[i]) {
    --i;
    }
    --i;
    --array[i];
    }
    ++i;
    ++i;
    putchar(array[i]);
    ++i;
    --array[i];
    --array[i];
    --array[i];
    putchar(array[i]);
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    ++array[i];
    putchar(array[i]);
    putchar(array[i]);
    ++array[i];
    ++array[i];
    ++array[i];
    putchar(array[i]);
    ++i;
    ++i;
    putchar(array[i]);
    --i;
    --array[i];
    putchar(array[i]);
    --i;
    putchar(array[i]);
    ++array[i];
    ++array[i];
    ++array[i];
    putchar(array[i]);
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    putchar(array[i]);
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    --array[i];
    putchar(array[i]);
    ++i;
    ++i;
    ++array[i];
    putchar(array[i]);
    ++i;
    ++array[i];
    ++array[i];
    putchar(array[i]);
}

